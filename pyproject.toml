[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jukebox-hic"
version = "0.2.3a"
description = "Hi-C noise analysis toolkit for .hic/.cool matrices"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "BSD-3-Clause"}
authors = [
  {name = "NoiseSampler Developers"}
]
dependencies = [
  "numpy>=1.21",
  "pandas>=1.3",
  "statsmodels>=0.13",
  "matplotlib>=3.5",
  "cooler>=0.9.3",
  "scipy>=1.8"
]

[project.optional-dependencies]
straw = ["hic-straw>=1.3.1"]
all = ["hic-straw>=1.3.1"]
profile = ["psutil>=5.9"]

[project.urls]
Homepage = "https://github.com/timrez/NoiseSampler"

[project.scripts]
jukebox-hic = "jukebox_hic.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
